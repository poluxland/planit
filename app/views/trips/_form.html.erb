<%= simple_form_for [@trip] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Submit", class: "btn btn-secondary" %>
<% end %>
