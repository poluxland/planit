console.log('enter ajax')
task = document.getElementById('<%= "#{@task.id}" %>')
console.log(task)
task.remove()

refresh('.overview-grab',`<%= render 'tasks/overview', trip: @trip %>`)

refresh('.progress-message[data-task-id="<%= @task.id %>"]',`<%= render "tasks/progress_message", task: @task %>`)

refreshById('check-<%= "#{@task.id}" %>',`<i class='far fa-<%= @task.status == true ? 'check-' : '' %>square' data-task-id = '<%= @task.id %>'></i>`)
