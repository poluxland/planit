<%= simple_form_for([task, subtask], remote: :true) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :status, as: :boolean %>
  <%= f.submit "Submit", class: "btn btn-secondary" %>
<% end %>
